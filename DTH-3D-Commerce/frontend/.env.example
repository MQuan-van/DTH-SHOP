# Copy to frontend/.env.local. All VITE_* values are public browser configuration.
# preview: bundled demo catalog; no account or server persistence.
# api: use the Express/MongoDB backend, through the Vite same-origin proxy.
VITE_STORE_MODE=api
VITE_SHOP_API_URL=/api/shop
